<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estado de Solicitud - DORADA EXPRESS</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 800px;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .header {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            padding: 30px;
            text-align: center;
        }

        .company-name {
            font-size: 2.5rem;
            font-weight: 900;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .content {
            padding: 40px;
        }

        .success-icon {
            text-align: center;
            font-size: 5rem;
            margin-bottom: 20px;
            animation: bounce 1s ease;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .title {
            text-align: center;
            font-size: 2rem;
            font-weight: 700;
            color: #27ae60;
            margin-bottom: 30px;
        }

        .info-card {
            background: linear-gradient(145deg, #f8f9fa 0%, #ffffff 100%);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 15px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            font-weight: 600;
            color: #34495e;
            font-size: 1.1rem;
        }

        .info-value {
            color: #7f8c8d;
            font-size: 1.1rem;
        }

        .status-badge {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.9rem;
        }

        .status-pending {
            background: linear-gradient(135deg, #f39c12, #f1c40f);
            color: white;
        }

        .status-assigned {
            background: linear-gradient(135deg, #3498db, #5dade2);
            color: white;
        }

        .status-in_progress {
            background: linear-gradient(135deg, #9b59b6, #bb8fce);
            color: white;
        }

        .status-delivered {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
        }

        .status-cancelled {
            background: linear-gradient(135deg, #e74c3c, #ec7063);
            color: white;
        }

        .tracking-section {
            margin-top: 30px;
        }

        .tracking-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 20px;
            text-align: center;
        }

        .tracking-steps {
            position: relative;
            padding-left: 40px;
        }

        .tracking-step {
            position: relative;
            padding: 20px 0;
        }

        .tracking-step::before {
            content: '';
            position: absolute;
            left: -29px;
            top: 0;
            width: 2px;
            height: 100%;
            background: #e0e0e0;
        }

        .tracking-step:last-child::before {
            display: none;
        }

        .tracking-dot {
            position: absolute;
            left: -36px;
            top: 25px;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: #e0e0e0;
            border: 3px solid white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }

        .tracking-step.active .tracking-dot {
            background: #27ae60;
            animation: pulse 2s infinite;
        }

        .tracking-step.completed .tracking-dot {
            background: #3498db;
        }

        .tracking-text {
            font-weight: 600;
            color: #34495e;
        }

        .tracking-step.active .tracking-text {
            color: #27ae60;
        }

        .btn-back {
            display: block;
            width: 100%;
            padding: 15px;
            margin-top: 30px;
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            text-align: center;
            text-decoration: none;
            border-radius: 10px;
            font-weight: 700;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .btn-back:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(44, 62, 80, 0.3);
        }

        .auto-refresh {
            text-align: center;
            margin-top: 20px;
            color: #7f8c8d;
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            .company-name {
                font-size: 2rem;
            }

            .content {
                padding: 20px;
            }

            .info-row {
                flex-direction: column;
                gap: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="company-name">🏍️ DORADA EXPRESS</h1>
        </div>

        <div class="content">
            <div class="success-icon">✅</div>
            <h2 class="title">¡Solicitud Registrada Exitosamente!</h2>

            <div class="info-card">
                <div class="info-row">
                    <span class="info-label">📋 ID de Solicitud:</span>
                    <span class="info-value">#<%= @delivery.id %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">🚚 Tipo de Servicio:</span>
                    <span class="info-value"><%= @delivery.service_type.upcase %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">📱 Teléfono:</span>
                    <span class="info-value"><%= @delivery.phone %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">📍 Dirección:</span>
                    <span class="info-value"><%= @delivery.address %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">💰 Precio:</span>
                    <span class="info-value" style="font-weight: 700; color: #27ae60; font-size: 1.3rem;">$<%= number_with_delimiter(@delivery.price) %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">🕐 Solicitado:</span>
                    <span class="info-value"><%= @delivery.requested_at.strftime('%d/%m/%Y %I:%M %p') %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">📊 Estado:</span>
                    <span class="status-badge status-<%= @delivery.status %>">
                        <%=
                            case @delivery.status
                            when 'pending' then '⏳ PENDIENTE'
                            when 'assigned' then '👤 ASIGNADO'
                            when 'in_progress' then '🏍️ EN CAMINO'
                            when 'delivered' then '✅ ENTREGADO'
                            when 'cancelled' then '❌ CANCELADO'
                            end
                        %>
                    </span>
                </div>
                <% if @delivery.delivery_person.present? %>
                <div class="info-row">
                    <span class="info-label">🏍️ Domiciliario:</span>
                    <span class="info-value"><%= @delivery.delivery_person %></span>
                </div>
                <% end %>
            </div>

            <div class="tracking-section">
                <h3 class="tracking-title">📍 Seguimiento de Entrega</h3>
                <div class="tracking-steps">
                    <div class="tracking-step <%= 'completed' if @delivery.status != 'pending' %> <%= 'active' if @delivery.status == 'pending' %>">
                        <div class="tracking-dot"></div>
                        <div class="tracking-text">⏳ Solicitud recibida</div>
                    </div>
                    <div class="tracking-step <%= 'completed' if ['in_progress', 'delivered'].include?(@delivery.status) %> <%= 'active' if @delivery.status == 'assigned' %>">
                        <div class="tracking-dot"></div>
                        <div class="tracking-text">👤 Domiciliario asignado</div>
                    </div>
                    <div class="tracking-step <%= 'completed' if @delivery.status == 'delivered' %> <%= 'active' if @delivery.status == 'in_progress' %>">
                        <div class="tracking-dot"></div>
                        <div class="tracking-text">🏍️ En camino a tu dirección</div>
                    </div>
                    <div class="tracking-step <%= 'active' if @delivery.status == 'delivered' %>">
                        <div class="tracking-dot"></div>
                        <div class="tracking-text">✅ Entrega completada</div>
                    </div>
                </div>
            </div>

            <a href="<%= new_delivery_path %>" class="btn-back">← HACER OTRA SOLICITUD</a>

            <div class="auto-refresh">
                🔄 Esta página se actualiza automáticamente cada 30 segundos
            </div>
        </div>
    </div>

    <script>
        // Auto-refresh cada 30 segundos
        setTimeout(function() {
            location.reload();
        }, 30000);

        // Animación de entrada
        document.addEventListener('DOMContentLoaded', function() {
            const container = document.querySelector('.container');
            container.style.opacity = '0';
            container.style.transform = 'scale(0.9)';

            setTimeout(() => {
                container.style.transition = 'all 0.5s ease';
                container.style.opacity = '1';
                container.style.transform = 'scale(1)';
            }, 100);
        });
    </script>
</body>
</html>
