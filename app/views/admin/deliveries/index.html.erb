<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Administrativo - DORADA EXPRESS</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1600px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.98);
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
            padding: 30px;
            text-align: center;
            color: white;
        }

        .company-name {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .panel-subtitle {
            font-size: 1.3rem;
            font-weight: 600;
            opacity: 0.95;
        }

        .main-content {
            padding: 40px;
        }

        .stats-bar {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-box {
            background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
            color: white;
            padding: 25px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(52, 152, 219, 0.3);
            transition: transform 0.3s ease;
        }

        .stat-box:hover {
            transform: translateY(-5px);
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .stat-box.pending {
            background: linear-gradient(135deg, #f39c12, #f1c40f);
        }

        .stat-box.assigned {
            background: linear-gradient(135deg, #9b59b6, #8e44ad);
        }

        .stat-box.in-progress {
            background: linear-gradient(135deg, #16a085, #1abc9c);
        }

        .stat-box.delivered {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
        }

        .section-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 3px solid #FFD700;
        }

        .filters {
            display: flex;
            gap: 15px;
            margin-bottom: 25px;
            flex-wrap: wrap;
        }

        .filter-btn {
            padding: 12px 25px;
            border: 2px solid #3498db;
            background: white;
            color: #3498db;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .filter-btn:hover, .filter-btn.active {
            background: #3498db;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }

        .deliveries-table {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background: linear-gradient(135deg, #34495e 0%, #2c3e50 100%);
            color: white;
        }

        th {
            padding: 20px 15px;
            text-align: left;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 0.9rem;
        }

        tbody tr {
            border-bottom: 1px solid #ecf0f1;
            transition: all 0.3s ease;
        }

        tbody tr:hover {
            background: #f8f9fa;
            transform: scale(1.01);
        }

        td {
            padding: 20px 15px;
            color: #34495e;
        }

        .status-badge {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.5px;
        }

        .status-pending {
            background: #f39c12;
            color: white;
        }

        .status-assigned {
            background: #3498db;
            color: white;
        }

        .status-in_progress {
            background: #9b59b6;
            color: white;
        }

        .status-delivered {
            background: #27ae60;
            color: white;
        }

        .status-cancelled {
            background: #e74c3c;
            color: white;
        }

        .action-form {
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .input-field {
            padding: 10px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            min-width: 150px;
        }

        .input-field:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }

        .select-field {
            padding: 10px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 0.9rem;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
            min-width: 120px;
        }

        .select-field:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }

        .btn-update {
            padding: 10px 20px;
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            font-size: 0.85rem;
        }

        .btn-update:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(39, 174, 96, 0.3);
        }

        .service-icon {
            font-size: 1.5rem;
            margin-right: 8px;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #7f8c8d;
        }

        .empty-state-icon {
            font-size: 5rem;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .empty-state-text {
            font-size: 1.3rem;
            font-weight: 600;
        }

        .timestamp {
            color: #95a5a6;
            font-size: 0.85rem;
        }

        .auto-refresh {
            text-align: center;
            margin-top: 30px;
            color: #7f8c8d;
            font-size: 0.9rem;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 10px;
        }

        @media (max-width: 1200px) {
            .main-content {
                padding: 20px;
            }

            .company-name {
                font-size: 2rem;
            }

            .deliveries-table {
                overflow-x: auto;
            }

            table {
                min-width: 1000px;
            }
        }

        @media (max-width: 768px) {
            .stats-bar {
                grid-template-columns: 1fr;
            }

            .action-form {
                flex-direction: column;
            }

            .input-field, .select-field {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="company-name">🏍️ DORADA EXPRESS</h1>
            <p class="panel-subtitle">🎯 PANEL ADMINISTRATIVO - GESTIÓN DE DOMICILIOS</p>
        </div>

        <div class="main-content">
            <div class="stats-bar">
                <div class="stat-box pending">
                    <div class="stat-number"><%= @deliveries.where(status: 'pending').count %></div>
                    <div class="stat-label">⏳ Pendientes</div>
                </div>
                <div class="stat-box assigned">
                    <div class="stat-number"><%= @deliveries.where(status: 'assigned').count %></div>
                    <div class="stat-label">👤 Asignados</div>
                </div>
                <div class="stat-box in-progress">
                    <div class="stat-number"><%= @deliveries.where(status: 'in_progress').count %></div>
                    <div class="stat-label">🏍️ En Camino</div>
                </div>
                <div class="stat-box delivered">
                    <div class="stat-number"><%= @deliveries.where(status: 'delivered').count %></div>
                    <div class="stat-label">✅ Entregados</div>
                </div>
            </div>

            <h2 class="section-title">📋 Solicitudes de Domicilio</h2>

            <div class="filters">
                <button class="filter-btn active" data-filter="all">📊 Todos</button>
                <button class="filter-btn" data-filter="pending">⏳ Pendientes</button>
                <button class="filter-btn" data-filter="assigned">👤 Asignados</button>
                <button class="filter-btn" data-filter="in_progress">🏍️ En Camino</button>
                <button class="filter-btn" data-filter="delivered">✅ Entregados</button>
            </div>

            <div class="deliveries-table">
                <% if @deliveries.any? %>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Servicio</th>
                            <th>Teléfono</th>
                            <th>Dirección</th>
                            <th>Estado</th>
                            <th>Fecha</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @deliveries.each do |delivery| %>
                        <tr class="delivery-row" data-status="<%= delivery.status %>">
                            <td><strong>#<%= delivery.id.to_s.last(6) %></strong></td>
                            <td>
                                <span class="service-icon">
                                    <%=
                                        case delivery.service_type
                                        when 'medicamentos' then '💊'
                                        when 'comida' then '🍕'
                                        when 'mercado' then '🛒'
                                        when 'paquetes' then '📦'
                                        end
                                    %>
                                </span>
                                <%= delivery.service_type.upcase %>
                            </td>
                            <td><%= delivery.phone %></td>
                            <td><%= truncate(delivery.address, length: 40) %></td>
                            <td>
                                <span class="status-badge status-<%= delivery.status %>">
                                    <%=
                                        case delivery.status
                                        when 'pending' then '⏳ PENDIENTE'
                                        when 'assigned' then '👤 ASIGNADO'
                                        when 'in_progress' then '🏍️ EN CAMINO'
                                        when 'delivered' then '✅ ENTREGADO'
                                        when 'cancelled' then '❌ CANCELADO'
                                        end
                                    %>
                                </span>
                            </td>
                            <td>
                                <div><%= delivery.created_at.strftime('%d/%m/%Y') %></div>
                                <div class="timestamp"><%= delivery.created_at.strftime('%H:%M') %></div>
                            </td>
                            <td>
                                <%= form_with model: delivery, url: admin_delivery_path(delivery), method: :patch, class: 'action-form', local: true do |f| %>
                                    <%= f.text_field :delivery_person,
                                        placeholder: delivery.delivery_person.presence || 'Nombre del domiciliario',
                                        class: 'input-field',
                                        value: delivery.delivery_person %>

                                    <%= f.select :status,
                                        options_for_select([
                                            ['⏳ Pendiente', 'pending'],
                                            ['👤 Asignado', 'assigned'],
                                            ['🏍️ En Camino', 'in_progress'],
                                            ['✅ Entregado', 'delivered'],
                                            ['❌ Cancelado', 'cancelled']
                                        ], delivery.status),
                                        {},
                                        class: 'select-field' %>

                                    <%= f.submit '💾 Actualizar', class: 'btn-update' %>
                                <% end %>
                            </td>
                        </tr>
                        <% end %>
                    </tbody>
                </table>
                <% else %>
                <div class="empty-state">
                    <div class="empty-state-icon">📭</div>
                    <div class="empty-state-text">No hay solicitudes de domicilio en este momento</div>
                </div>
                <% end %>
            </div>

            <div class="auto-refresh">
                🔄 Esta página se actualiza automáticamente cada 30 segundos
            </div>
        </div>
    </div>

    <script>
        // Auto-refresh cada 30 segundos
        setTimeout(function() {
            location.reload();
        }, 30000);

        // Sistema de filtros
        document.addEventListener('DOMContentLoaded', function() {
            const filterBtns = document.querySelectorAll('.filter-btn');
            const deliveryRows = document.querySelectorAll('.delivery-row');

            filterBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    // Remover active de todos los botones
                    filterBtns.forEach(b => b.classList.remove('active'));
                    // Agregar active al botón clickeado
                    this.classList.add('active');

                    const filter = this.getAttribute('data-filter');

                    deliveryRows.forEach(row => {
                        if (filter === 'all') {
                            row.style.display = '';
                        } else {
                            const status = row.getAttribute('data-status');
                            row.style.display = status === filter ? '' : 'none';
                        }
                    });
                });
            });

            // Animación de entrada
            const container = document.querySelector('.container');
            container.style.opacity = '0';
            container.style.transform = 'translateY(20px)';

            setTimeout(() => {
                container.style.transition = 'all 0.5s ease';
                container.style.opacity = '1';
                container.style.transform = 'translateY(0)';
            }, 100);
        });
    </script>
</body>
</html>
